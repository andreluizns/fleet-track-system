# =============================================================================
# Frontend — Variáveis de Ambiente (Next.js)
# =============================================================================
# INSTRUÇÕES:
#   1. Copie este arquivo: cp .env.example .env.local
#   2. NUNCA commite o arquivo .env.local no controle de versão
# =============================================================================

# URL do Tracking Worker (Socket.io + endpoints HTTP)
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001
